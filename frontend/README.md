# Frontend

Next.js UI. Общий запуск и env — в [корневом README](../README.md) и [`.env.example`](../.env.example).

```bash
cd frontend
cp .env.example .env.local
npm ci
npm run dev
```

UI: http://localhost:3001  

Полный стек через Docker: из корня `npm run up` → http://localhost
