# Backend

NestJS API. Общий запуск и env — в [корневом README](../README.md) и [`.env.example`](../.env.example).

```bash
# из корня репозитория
npm run db:up
cd backend && npm ci && npm run start:dev
```

Swagger (локально): http://localhost:3000/api/docs
